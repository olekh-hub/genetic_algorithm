[project]
name = "ga-core"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["numpy", "pandas"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ga_core"]
