[project]
name = "p1-binary"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["ga-core"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/p1_binary"]

[tool.uv.sources]
ga-core = { workspace = true }
