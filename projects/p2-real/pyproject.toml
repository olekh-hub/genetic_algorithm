[project]
name = "p2-real"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["ga-core"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/p2_real"]

[tool.uv.sources]
ga-core = { workspace = true }
